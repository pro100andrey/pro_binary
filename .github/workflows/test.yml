name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        sdk: [stable, beta]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: ${{ matrix.sdk }}
      
      - name: Print Dart version
        run: dart --version
      
      - name: Install dependencies
        run: dart pub get
      
      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .
      
      - name: Analyze code
        run: dart analyze --fatal-infos
      
      - name: Run tests
        run: dart test -x benchmark
      
      - name: Run tests with coverage
        if: matrix.os == 'ubuntu-latest' && matrix.sdk == 'stable'
        run: |
          dart pub global activate coverage
          dart pub global run coverage:test_with_coverage -- -x benchmark
      
      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.sdk == 'stable'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage/lcov.info
          fail_ci_if_error: false

  benchmark:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get

      - name: Run Benchmarks (JIT)
        run: |
          dart run benchmark_harness:bench --flavor jit --target test/performance/serialization_bench.dart
          dart run benchmark_harness:bench --flavor jit --target test/performance/deserialization_bench.dart
          dart run benchmark_harness:bench --flavor jit --target test/performance/strings_bench.dart
          dart run benchmark_harness:bench --flavor jit --target test/performance/pool_bench.dart

      - name: Run Benchmarks (AOT)
        run: |
          dart run benchmark_harness:bench --flavor aot --target test/performance/serialization_bench.dart
          dart run benchmark_harness:bench --flavor aot --target test/performance/deserialization_bench.dart
          dart run benchmark_harness:bench --flavor aot --target test/performance/strings_bench.dart
          dart run benchmark_harness:bench --flavor aot --target test/performance/pool_bench.dart

      - name: Run Benchmarks (JS)
        run: |
          dart run benchmark_harness:bench --flavor js --target test/performance/serialization_bench.dart
          dart run benchmark_harness:bench --flavor js --target test/performance/deserialization_bench.dart
          dart run benchmark_harness:bench --flavor js --target test/performance/strings_bench.dart
          dart run benchmark_harness:bench --flavor js --target test/performance/pool_bench.dart

      - name: Run Benchmarks (WASM)
        run: |
          dart run benchmark_harness:bench --flavor wasm --target test/performance/serialization_bench.dart
          dart run benchmark_harness:bench --flavor wasm --target test/performance/deserialization_bench.dart
          dart run benchmark_harness:bench --flavor wasm --target test/performance/strings_bench.dart
          dart run benchmark_harness:bench --flavor wasm --target test/performance/pool_bench.dart
